pub fn npc_stunrun_high(character_id: usize) -> i32 {
    0
}

pub fn npc_stunrun_low(character_id: usize) -> i32 {
    0
}

pub fn npc_stunrun_msg(
    character_id: usize,
    msg_type: i32,
    dat1: i32,
    dat2: i32,
    dat3: i32,
    dat4: i32,
) -> i32 {
    0
}

pub fn npc_cityattack_high(character_id: usize) -> i32 {
    0
}

pub fn npc_cityattack_low(character_id: usize) -> i32 {
    0
}

pub fn npc_cityattack_msg(
    character_id: usize,
    msg_type: i32,
    dat1: i32,
    dat2: i32,
    dat3: i32,
    dat4: i32,
) -> i32 {
    0
}

pub fn npc_malte_high(character_id: usize) -> i32 {
    0
}

pub fn npc_malte_low(character_id: usize) -> i32 {
    0
}

pub fn npc_malte_msg(
    character_id: usize,
    msg_type: i32,
    dat1: i32,
    dat2: i32,
    dat3: i32,
    dat4: i32,
) -> i32 {
    0
}
