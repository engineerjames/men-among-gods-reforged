# SDL2 Starter (Rust)

Minimal `rust-sdl2` starter project inside the workspace.

## Prerequisites (macOS)

```bash
brew install sdl2
```

## Build

```bash
cargo build -p sdl_starter
```

## Run

```bash
cargo run -p sdl_starter
```

Controls:
- `Esc` or close window to quit.
